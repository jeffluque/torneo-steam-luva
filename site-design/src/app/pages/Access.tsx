import{useState}from"react";import{useNavigate}from"react-router";import{LockKeyhole,UserRoundCheck}from"lucide-react";import{login}from"../lib/demoStore";
export function Access(){const navigate=useNavigate(),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");setLoading(true);try{const session=await login(username,password);navigate(session.role==="admin"?"/admin":"/juez")}catch(error){setError(error instanceof Error?error.message:"No se pudo iniciar sesión.")}finally{setLoading(false)}};return <div className="min-h-screen bg-background flex items-center justify-center p-4"><div className="w-full max-w-md bg-surface border border-border rounded-3xl p-8 shadow-2xl"><div className="w-16 h-16 bg-primary/15 rounded-2xl flex items-center justify-center mx-auto mb-5"><UserRoundCheck className="w-8 h-8 text-primary"/></div><h1 className="text-2xl font-bold text-center">Acceso del torneo</h1><p className="text-text-muted text-center mt-2 mb-8">Jueces y administración · STEAM LUVÁ 2026</p><form onSubmit={submit} className="space-y-5"><label className="block text-sm text-text-muted">Usuario<input required autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 w-full bg-background border border-border rounded-xl px-4 py-3 text-white"/></label><label className="block text-sm text-text-muted">Contraseña<div className="relative mt-2"><LockKeyhole className="absolute left-3 top-3.5 w-5 h-5 text-text-muted"/><input required autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full bg-background border border-border rounded-xl pl-10 pr-4 py-3 text-white"/></div></label>{error&&<p className="text-red-400 text-sm">{error}</p>}<button disabled={loading} className="w-full bg-primary hover:bg-primary-hover disabled:opacity-50 py-3.5 rounded-xl font-bold">{loading?"Verificando...":"Ingresar"}</button></form><p className="mt-7 text-xs text-center text-text-muted">Acceso restringido. Las credenciales son administradas por la organización.</p><a href="/" className="block text-center text-sm text-text-muted hover:text-white mt-6">Volver al sitio público</a></div></div>}
