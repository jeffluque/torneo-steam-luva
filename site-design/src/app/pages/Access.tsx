import {useState} from "react";import{useNavigate}from"react-router";import{LockKeyhole,UserRoundCheck}from"lucide-react";import{login}from"../lib/demoStore";
export function Access(){const navigate=useNavigate(),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const submit=(e:React.FormEvent)=>{e.preventDefault();const session=login(username,password);if(!session)return setError("Usuario o contraseña incorrectos.");navigate(session.role==="admin"?"/admin":"/juez")};return <div className="min-h-screen bg-background flex items-center justify-center p-4"><div className="w-full max-w-md bg-surface border border-border rounded-3xl p-8 shadow-2xl"><div className="w-16 h-16 bg-primary/15 rounded-2xl flex items-center justify-center mx-auto mb-5"><UserRoundCheck className="w-8 h-8 text-primary"/></div><h1 className="text-2xl font-bold text-center">Acceso del torneo</h1><p className="text-text-muted text-center mt-2 mb-8">Jueces y administración · STEAM LUVÁ 2026</p><form onSubmit={submit} className="space-y-5"><label className="block text-sm text-text-muted">Usuario<input required value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 w-full bg-background border border-border rounded-xl px-4 py-3 text-white" placeholder="juez.ana"/></label><label className="block text-sm text-text-muted">Contraseña<div className="relative mt-2"><LockKeyhole className="absolute left-3 top-3.5 w-5 h-5 text-text-muted"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full bg-background border border-border rounded-xl pl-10 pr-4 py-3 text-white"/></div></label>{error&&<p className="text-red-400 text-sm">{error}</p>}<button className="w-full bg-primary hover:bg-primary-hover py-3.5 rounded-xl font-bold">Ingresar</button></form><div className="mt-7 p-4 bg-background rounded-xl text-xs text-text-muted space-y-1"><p className="font-bold text-white">Credenciales de demostración</p><p>Juez: juez.ana / Juez2026!</p><p>Admin: admin / Admin2026!</p></div><a href="/" className="block text-center text-sm text-text-muted hover:text-white mt-6">Volver al sitio público</a></div></div>}
