import{ClipboardCheck,History,LogOut}from"lucide-react";import{NavLink,Outlet,useNavigate}from"react-router";import{getSession,logout}from"../../lib/demoStore";
export function JudgeLayout(){const session=getSession(),navigate=useNavigate();return <div className="min-h-screen bg-background text-text"><header className="border-b border-border bg-surface sticky top-0 z-20"><div className="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between gap-4"><div><p className="text-xs text-cyan uppercase tracking-widest">Portal de jueces</p><h1 className="font-bold">{session?.name}</h1></div><nav className="flex items-center gap-2"><NavLink end to="/juez" className={({isActive})=>`px-3 py-2 rounded-lg text-sm flex gap-2 ${isActive?'bg-primary text-white':'text-text-muted'}`}><ClipboardCheck className="w-4 h-4"/>Evaluar</NavLink><NavLink to="/juez/historial" className={({isActive})=>`px-3 py-2 rounded-lg text-sm flex gap-2 ${isActive?'bg-primary text-white':'text-text-muted'}`}><History className="w-4 h-4"/>Historial</NavLink><button aria-label="Cerrar sesión" onClick={()=>{logout();navigate('/acceso')}} className="p-2 text-text-muted hover:text-red-400"><LogOut className="w-5 h-5"/></button></nav></div></header><main className="max-w-6xl mx-auto p-4 md:p-8"><Outlet/></main></div>}
